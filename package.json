{
  "name": "cognilens",
  "version": "1.0.0",
  "description": "CogniLens — Multimodal Agentic AI Study Assistant",
  "private": true,
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm run build --prefix client"
  }
}
